# copied from ChatGPT, GPT-5
environment: test
spring:
  application:
    name: backend
  cloud:
    gcp:
      project-id: qdog-6aca2
      credentials:
        enabled: false
      firestore:
        emulator:
          enabled: true
          host-port: localhost:8080

  data:
    rest:
      detection-strategy: annotated

logging:
  level:
    root: WARN
    com.softeng.backend: INFO

server:
  port: 0  # random port for tests
