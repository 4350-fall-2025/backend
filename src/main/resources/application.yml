spring:
  application:
    name: backend
  cloud:
    gcp:
      firestore:
        emulator:
          enabled: true
        project-id: qdog-6aca2
logging:
  level:
    root: INFO
    com.softeng.backend: DEBUG # Change to INFO if you don't want debug logs

server:
  port: 3000
  compression:
    enabled: true
  error:
    include-message: always
  shutdown: "graceful"
