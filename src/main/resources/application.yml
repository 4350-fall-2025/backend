spring:
  application:
    name: backend
  cloud:
    gcp:
      firestore:
        emulator:
          enabled: true
        project-id: qdog-6aca2
  config:
    import: optional:classpath:.properties
server:
  port: 3000
  compression:
    enabled: true
  error:
    include-message: always
  shutdown: "graceful"